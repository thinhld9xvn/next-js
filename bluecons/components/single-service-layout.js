import React from 'preact/compat'
import SingleProductLayout from './single-product-layout'
export default function SingleServiceLayout({ pageContext }) {
    return (
        <SingleProductLayout pageContext = {pageContext} />
    )
}
